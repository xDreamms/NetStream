<hc:Window x:Class="NetStream.Views.InstallerWindow"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
           xmlns:local="clr-namespace:NetStream.Views"
           xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
           xmlns:hc="https://handyorg.github.io/handycontrol"
           mc:Ignorable="d"
           Title="NetStream" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" Height="600" Width="1000" Background="#111111" >
    <materialDesign:DialogHost x:Name="DialogHost" >
        <materialDesign:DialogHost.DialogContent>
            <StackPanel Margin="15">
                <ProgressBar 
                    IsIndeterminate="True"
                    Style="{StaticResource MaterialDesignCircularProgressBar}"
                    Value="0"
                />
                <TextBlock x:Name="TextBlockInfo" FontSize="15" Margin="0 20 0 0" Text="Installing python..."></TextBlock>
            </StackPanel>
        </materialDesign:DialogHost.DialogContent>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="0.20*"></RowDefinition>
                <RowDefinition Height="0.3*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>



            <Image Grid.Row="1" VerticalAlignment="Center"  Grid.Column="0"  HorizontalAlignment="Center"  Stretch="Uniform" Source="../logo/NetStreamLogo.png"></Image>


            <Frame BorderThickness="0" NavigationUIVisibility="Hidden" x:Name="InstallerFrame" Grid.Column="0" Grid.Row="2" ></Frame>
        </Grid>
    </materialDesign:DialogHost>

</hc:Window>
