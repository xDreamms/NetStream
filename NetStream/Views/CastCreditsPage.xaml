<Page x:Class="NetStream.CastCreditsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NetStream"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CastCreditsPage" TextElement.Foreground="{DynamicResource MaterialDesignBody}"
      TextElement.FontWeight="Regular"
      TextElement.FontSize="13"
      TextOptions.TextFormattingMode="Ideal"
      TextOptions.TextRenderingMode="Auto"
      FontFamily="{DynamicResource MaterialDesignFont}"
      Background="#111111" Loaded="CastCreditsPage_OnLoaded" Unloaded="CastCreditsPage_OnUnloaded">

        <Grid>
           <Grid.RowDefinitions>
               <RowDefinition Height="0.2*"></RowDefinition>
               <RowDefinition Height="0.1*"></RowDefinition>
               <RowDefinition Height="*"></RowDefinition>
               <RowDefinition Height="50"></RowDefinition>
                <RowDefinition Height="0.2*"></RowDefinition>
               <RowDefinition Height="0.1*"></RowDefinition>
               <RowDefinition Height="*"></RowDefinition>
           </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Viewbox Grid.Column="0" HorizontalAlignment="Left">
                <TextBlock FontSize="23" Text="{DynamicResource ActingString}" Foreground="White" ></TextBlock>
                </Viewbox>
            </Grid>

        <ListBox Grid.Row="2" HorizontalContentAlignment="Stretch"   ScrollViewer.CanContentScroll="True" 
VirtualizingPanel.IsVirtualizing="true" 
VirtualizingPanel.VirtualizationMode="Recycling"   SelectionChanged="CreditsActingDisplay_OnSelectionChanged"  ScrollViewer.ScrollChanged="CreditsActingDisplay_OnScrollChanged"  ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" Background="Transparent" x:Name="CreditsActingDisplay">

            <ItemsControl.Resources>
                <Style x:Key="ScaleStyle" TargetType="Image">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Grid.ZIndex" Value="1"/>
                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ItemsControl.Resources>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="IsSelected" Value="{Binding Content.IsSelected, Mode=TwoWay, RelativeSource={RelativeSource Self}}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="{Binding ActualWidth, ElementName=CreditsActingDisplay}"
                                Orientation="Vertical" Background="#181819" Cursor="Hand"  Margin="0 10 20 0" >
                        <TextBlock FontSize="18" Margin="15 15 0 5" Foreground="White" Text="{Binding ReleaseDate}"></TextBlock>
                        <TextBlock FontSize="18" Margin="15 0 0 5" Foreground="#FEFEFE" Text="{Binding Name}"></TextBlock>
                        <TextBlock FontSize="18" Margin="15 0 0 15" Foreground="#8B8B8C" Text="{Binding Role}"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel VirtualizingStackPanel.IsVirtualizing="True"  VirtualizingStackPanel.VirtualizationMode="Recycling" IsItemsHost="True" Orientation="Horizontal"  />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>


            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Viewbox Grid.Column="0" HorizontalAlignment="Left">
                <TextBlock FontSize="23" Text="{DynamicResource ProductionString}" Foreground="White" ></TextBlock>
                </Viewbox>
            </Grid>

        <ListBox Grid.Row="6" HorizontalContentAlignment="Stretch"   ScrollViewer.CanContentScroll="True" 
VirtualizingPanel.IsVirtualizing="true" 
VirtualizingPanel.VirtualizationMode="Recycling"   SelectionChanged="CreditsProductionDisplay_OnSelectionChanged"  ScrollViewer.ScrollChanged="CreditsProductionDisplay_OnScrollChanged"  ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" Background="Transparent" x:Name="CreditsProductionDisplay">

            <ItemsControl.Resources>
                <Style x:Key="ScaleStyle" TargetType="Image">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Grid.ZIndex" Value="1"/>
                            <Setter Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ItemsControl.Resources>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="IsSelected" Value="{Binding Content.IsSelected, Mode=TwoWay, RelativeSource={RelativeSource Self}}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Width="{Binding ActualWidth, ElementName=CreditsActingDisplay}"
                                Orientation="Vertical" Background="#181819" Cursor="Hand"  Margin="0 10 20 0" >
                        <TextBlock FontSize="18" Margin="15 15 0 5" Foreground="White" Text="{Binding ReleaseDate}"></TextBlock>
                        <TextBlock FontSize="18" Margin="15 0 0 5" Foreground="#FEFEFE" Text="{Binding Name}"></TextBlock>
                        <TextBlock FontSize="18" Margin="15 0 0 15" Foreground="#8B8B8C" Text="{Binding Job}"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel VirtualizingStackPanel.IsVirtualizing="True"  VirtualizingStackPanel.VirtualizationMode="Recycling" IsItemsHost="True" Orientation="Horizontal"  />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>

    </Grid>
</Page>
