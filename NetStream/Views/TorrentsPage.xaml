<Page x:Class="NetStream.TorrentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NetStream"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:controls="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel"
      xmlns:hc="https://handyorg.github.io/handycontrol"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TorrentsPage"  TextElement.Foreground="{DynamicResource MaterialDesignBody}"
      TextElement.FontWeight="Regular"
      TextElement.FontSize="13"
      TextOptions.TextFormattingMode="Ideal"
      TextOptions.TextRenderingMode="Auto"
      FontFamily="{DynamicResource MaterialDesignFont}"
      Background="#111111" Loaded="TorrentsPage_OnLoaded" Unloaded="TorrentsPage_OnUnloaded" KeepAlive="False">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.07*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="10 0 0 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StackPanel Margin="0 20 0 20" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBlock FontSize="28" Foreground="White" Style="{StaticResource Gotham}" Text="{DynamicResource Torrents}"></TextBlock>
                <TextBlock Style="{StaticResource Gotham}" Margin="5 12 0 0" x:Name="TorrentCount" Foreground="#888888" Text=""></TextBlock>
            </StackPanel>
            <ComboBox FontFamily="/Fonts/#Gotham" Width="230" HorizontalAlignment="Right" Foreground="White" x:Name="SortByComboBox" Grid.Column="2"
                      materialDesign:HintAssist.Hint="{DynamicResource SortString}" SelectionChanged="SortByComboBox_OnSelectionChanged"
            >
            </ComboBox>

        </Grid>
        <StackPanel x:Name="SearchingPanel" Orientation="Vertical" Grid.Row="1" VerticalAlignment="Center">
            <hc:LoadingCircle Foreground="White" Grid.Row="1" Height="100" Width="100" />
            <TextBlock Margin="0 30 0 0" Text="{DynamicResource Searching}" Foreground="White" HorizontalAlignment="Center" FontSize="24"></TextBlock>
        </StackPanel>
        
        <ListBox Grid.Row="1"   ScrollViewer.CanContentScroll="True" 
                 VirtualizingPanel.IsVirtualizing="true" 
                 VirtualizingPanel.VirtualizationMode="Recycling"   SelectionChanged="TorrentsDisplay_OnSelectionChanged"  ScrollViewer.ScrollChanged="TorrentsDisplay_OnScrollChanged"  ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" Background="Transparent" x:Name="TorrentsDisplay">

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="IsSelected" Value="{Binding Content.IsSelected, Mode=TwoWay, RelativeSource={RelativeSource Self}}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>

                    <StackPanel Orientation="Horizontal" Cursor="Hand"  Margin="10 5 20 30" >


                        <Border BorderBrush="#1F2021" VerticalAlignment="Top" MaxHeight="275" MaxWidth="300" HorizontalAlignment="Left" RenderTransformOrigin="0.5,0.5" BorderThickness="5">
                            <Image   Source="{Binding Poster}"></Image>
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="ImageScale" ScaleX="1" ScaleY="1" ></ScaleTransform>
                            </Border.RenderTransform>
                            <Border.Triggers>
                                <EventTrigger RoutedEvent="Image.MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ImageScale" Storyboard.TargetProperty="(ScaleTransform.ScaleX)" To="1.05" Duration="0:0:0.20" />
                                            <DoubleAnimation Storyboard.TargetName="ImageScale" Storyboard.TargetProperty="(ScaleTransform.ScaleY)" To="1.05" Duration="0:0:0.20" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger RoutedEvent="MouseLeave">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ImageScale" Storyboard.TargetProperty="(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.20" />
                                            <DoubleAnimation Storyboard.TargetName="ImageScale" Storyboard.TargetProperty="(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.20" />
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Border.Triggers>
                        </Border>

                        <StackPanel Margin="0 20 0 0" Orientation="Vertical">
                            <TextBlock FontWeight="Normal" Style="{StaticResource Gotham}" FontSize="20" Margin="20 0 0 0"   Grid.Row="1" Grid.Column="2" Width="700" TextWrapping="Wrap" Text="{Binding Title}"></TextBlock>

                            <TextBlock FontWeight="Light" Style="{StaticResource Gotham}"  FontSize="15" HorizontalAlignment="Left" Margin="20 10 0 0"  Grid.Row="3" Grid.Column="2" Text="{Binding PublishDate}"></TextBlock>

                            <TextBlock FontWeight="Light" Style="{StaticResource Gotham}" FontSize="15" HorizontalAlignment="Left" Margin="20 5 0 0"  Grid.Row="4" Grid.Column="2" Text="{Binding SizeProperty}"></TextBlock>

                            <TextBlock FontWeight="Light" Style="{StaticResource Gotham}" FontSize="15" HorizontalAlignment="Left" Margin="20 5 0 0"  Grid.Row="6" Grid.Column="2" Text="{Binding SeedersProperty}"></TextBlock>
                        </StackPanel>
                       


                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <controls:VirtualizingWrapPanel  AllowDifferentSizedItems="True" HorizontalAlignment="Left" ScrollUnit="Pixel" CacheLengthUnit="Page" CacheLength="1.1" ScrollLineDelta="16" MouseWheelDelta="48"  SpacingMode="Uniform" VirtualizingStackPanel.IsVirtualizing="True"  VirtualizingStackPanel.VirtualizationMode="Recycling" IsItemsHost="True" Orientation="Horizontal"  />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</Page>
